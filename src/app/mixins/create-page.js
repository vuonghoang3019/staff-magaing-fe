import {ContentHeader, Tabs} from "@/app/core/components";

export default {
    components: {
        ContentHeader,
        Tabs
    },

    setup() {
        const create = () => {

        }

        return {
            create
        }
    }

    // methods: {
    //     create() {},
    // },

    // created() {
    //     this.create();
    // },
}