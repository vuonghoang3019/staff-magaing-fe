import {ContentHeader, Tabs} from "@/app/core/components";

export default {
    components: {
        ContentHeader,
        Tabs
    },

    setup() {
        const edit = () => {

        };

        return {
            edit
        }
    }
}